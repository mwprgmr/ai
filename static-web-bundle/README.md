# Crop Tech AI Static Web Bundle

This bundle has a real root `index.html` for static hosts.

Open or deploy:

```text
index.html
```

The page loads without a build step. Live AI responses require a compatible `/api/chat` backend. For Vercel with API support, deploy the Next.js source bundle in `vercel-web-bundle/crop-tech-ai-source`.
